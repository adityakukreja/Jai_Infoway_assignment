import { Injectable } from "@angular/core";
import { Observable } from 'rxjs';

@Injectable({
    providedIn:'root'
})
//Demo Service Class
export class LoginService {
    // constructor(private http : HttpClient){

    // }

    // storeLoginDetails():Observable<outputEntity>{
    //     return this.http.post<outputEntity>(ServiceUrl,Body,)
    //     .pipe(map((res)=>{
    //         return res;
    //     }))
    // }
}